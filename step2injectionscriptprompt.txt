You are a Site Reliability Engineer (SRE) and DBA expert.

Goal:
Create a safe fault injection script to simulate database performance and availability issues for testing ( Postgres 13)

Environment:
- I already have infrastructure running (App server with public ipaddress connecting to db server ( private server using clinet
- Database is running on a VM (Linux preferred)
- App server connects to DB

Requirements:

Create a script (bash or simple SQL + shell) that can simulate the following faults:

1. Connection Pool Exhaustion: 
   - Open a large number of database connections
   - Hold them without closing
   - Cause new connections to fail or slow down

2. Runaway Query:
   - Run a long-running or heavy query (e.g., full table scan, CPU-heavy loop)
   - Consume CPU or I/O resources on the DB server

3. Optional (nice to have):
   - Lock a table to cause blocking
   - Simulate slow response (sleep/delay queries)

Script Requirements:
- Must be SAFE (easy to stop) and run from App server 
- Include a cleanup/rollback command to restore normal state
- Clearly explain what each step does
- Should NOT permanently damage data
- Limit impact (controlled duration, controlled load)

Output should include:
- PostgreSQL 
- Step-by-step script
- How to run the script
- How to monitor impact (CPU, connections, query time)
- How to stop/cleanup quickly

Keep it simple and practical for testing in a lab environment.